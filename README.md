# NetworkPacket-Analyzer
A full-stack network packet analyzer that performs live packet sniffing on your Wi-Fi interface via a Python Flask + WebSocket backend, and streams the data in real time to a React dashboard with interactive charts, filters, and export functionality. Inspired by Wireshark.
